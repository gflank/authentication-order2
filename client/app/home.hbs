<div class="navbar navbar-default navbar-static-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#navbar-ex-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="/"><span>Test</span></a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="navbar-ex-collapse">
      <ul class="nav navbar-nav navbar-right">
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">Account<i class="fa fa-caret-down"></i></a>
          <ul class="dropdown-menu" role="menu">
            <li>
            {{#if user}}
              <a href="profile">{{user.username}}</a>
            </li>
            <li class="divider"></li>
            <li>
              <a href="/logout">Log Out</a>
            {{else}}
              <a href="/signin">Sign in</a>
            </li>
            <li>
              <a href="signup">Sign up</a>
            {{/if}}
            </li>
          </ul>
        </li>
        {{#if user}}
        <li>
          <a herf="order">Order</a>
        </li>
         <li>
          <a herf="/inventory">Inventory</a>
        </li>
         <li>
          <a herf="/product">Product</a>
        </li>
        {{/if}}
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container -->

  {{#if error}}
    <p class="alert alert-warning">{{error}}</p>
  {{/if}}

  {{#if success}}
    <p class="alert alert-success">{{success}}</p>
  {{/if}}

  {{#if notice}}
    <p class="alert alert-info">{{notice}}</p>
  {{/if}}

</div>
